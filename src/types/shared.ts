export type Car = {
  carName: string;
  carType: string;
  rent: number;
  capacity: number;
  transmission: string;
  image: string;
};

export type dashBoardData = {
  value: number;
  name: string;
  maxValue: number;
};
